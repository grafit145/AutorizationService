events {
    worker_connections  1024;
}

http {
	server {
		listen  80;
		server_name  localhost;

		location /signin{
			root html;
			index logform.html;
		}

		location / {
			proxy_pass http://localhost:8080/;
		}

	}
}